#ifndef BENCHMARK_MAP_HPP
# define BENCHMARK_MAP_HPP

class benchmark_map
{
    public:
        benchmark_map();
        virtual ~benchmark_map();
        benchmark_map(benchmark_map const &other);
        benchmark_map &operator=(benchmark_map const &other);

    private:
};

#endif
